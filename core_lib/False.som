False = Boolean (

    asString = ( ^'false' )

    ifTrue: aBlock = ( ^nil )
    ifFalse: aBlock = ( ^aBlock value )

    not = ( ^true )
    or: aBlock = ( ^aBlock value )
    and: aBlock = ( ^false )

)